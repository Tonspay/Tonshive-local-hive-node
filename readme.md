# Tonshive local hiver

This repo is to storage a puppeteer base local hive

Do remember to install the `chromium-browser` before try run it .

For Ubuntu
```
apt install chromium-browser
```

Install the package
```
npm install
```

Final . run it 
```
node hive.js
```